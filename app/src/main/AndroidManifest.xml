<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chileme">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".ChangeInformation"></activity>
        <activity android:name=".UserInformation"></activity>
        <activity android:name=".MainFragment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainSpinnerActivity"></activity>
        <activity android:name=".MainTopActivity"></activity>
        <activity android:name=".MainViewActivity">

        </activity>
        <activity android:name=".ShopFoodActivity">

        </activity>
        <activity android:name=".ShopFoodSpinnerActivity"></activity>
    </application>

</manifest>